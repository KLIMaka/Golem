import java.util.regex.*;
import java.util.*;
import java.net.*;
import gnu.bytecode.*;
import gnu.bytecode.ClassType.*;
import golem.frontend.*;

def r = Math.random;
def abs = Math.abs; 
def time = System.currentTimeMillis;
def print = System.out.println;

def i = 0;
def rand = abs(r() - r());
while (rand > 0.001) {
	rand = abs(r() - r());
	i = i + 1;
}

System.out.println(i);
System.out.println(rand);
